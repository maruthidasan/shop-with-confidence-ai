
GEMINI_API_KEY=replcae with api key
# Model identifier used by the Senior Fashion Stylist service.
GEMINI_MODEL=gemini-3.5-flash-lite
GEMINI_FALLBACK_MODEL=gemini-3.6-flash
# Perfect Corp / YouCam server-side secret. Required only when AI_MODE=live.
YOUCAM_API_KEY=replace with api key
# mock consumes no provider credits; live activates configured provider clients.
AI_MODE=mock
