"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
const messages = ["Understanding your face...", "Understanding skin tone...", "Finding matching outfits...", "Reducing decision fatigue...", "Preparing recommendations..."];
export default function AnalysisLoader() { const [index, setIndex] = useState(0); const router = useRouter(); useEffect(() => { const cycle = setInterval(() => setIndex(i => i < messages.length - 1 ? i + 1 : i), 2000); const done = setTimeout(() => router.push("/recommendations"), 10000); return () => { clearInterval(cycle); clearTimeout(done); }; }, [router]); return <div className="text-center"><div className="relative mx-auto grid h-40 w-40 place-items-center"><div className="absolute inset-0 rounded-full border-2 border-violet-200"/><div className="absolute inset-3 rounded-full border-2 border-t-violet-600 border-r-violet-400 border-b-transparent border-l-transparent animate-spin"/><div className="grid h-20 w-20 place-items-center rounded-full bg-white text-3xl shadow-lg">✦</div></div><p className="mt-10 text-sm font-medium tracking-[.18em] text-violet-600">FASHION RETAIL AI IS THINKING</p><h1 className="mt-4 min-h-10 text-2xl font-semibold tracking-tight sm:text-3xl">{messages[index]}</h1><p className="mt-3 text-slate-500">Curating a smaller, smarter set of choices for you.</p><div className="mx-auto mt-10 flex justify-center gap-2">{messages.map((_,i) => <span key={i} className={`h-1.5 rounded-full transition-all ${i <= index ? "w-7 bg-violet-600" : "w-1.5 bg-violet-200"}`} />)}</div></div>; }
